name: scene_dash_v2_core
description: >-
  Scene-Dash v2 headless core: organize, coordinate, and test gameplay code
  with an ECS runtime built on sparse sets, generational entities, events,
  clocks, command buffers, lifecycle hooks, GameState, and TestGame.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.18.3

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
